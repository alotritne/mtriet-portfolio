import type { CalculatorResult } from '../../types/calculator'
import { formatUSD, formatVND } from '../../utils/calculator'
export function CompareCard({ result }: { result: CalculatorResult }) { const cryptoWins = result.receiveUSDT > result.receiveBank; const rows = [['Bank', result.receiveBank, result.receiveBankVND, result.bankTotalFee, result.bankFeePercent, !cryptoWins], ['USDT', result.receiveUSDT, result.receiveUSDTVND, result.cryptoTotalFee, result.cryptoFeePercent, cryptoWins]] as const; return <section className="eld-card eld-compare"><div className="eld-card-heading"><div><span className="eld-eyebrow">Take-home comparison</span><h2>Two exits, one decision.</h2></div></div><div className="eld-table-wrap"><table><thead><tr><th>Route</th><th>USD</th><th>VND</th><th>Total fee</th><th>Fee %</th><th>Winner</th></tr></thead><tbody>{rows.map(([route, usd, vnd, fee, percent, winner]) => <tr className={winner ? 'is-winner' : ''} key={route}><th>{route}</th><td>{formatUSD(usd)}</td><td>{formatVND(vnd)}</td><td>{formatUSD(fee)}</td><td>{percent.toFixed(2)}%</td><td>{winner ? '🏆' : '—'}</td></tr>)}</tbody></table></div></section> }
