export function SummaryCard({ copied, onCopyResult, onCopyFormula }: { copied: string; onCopyResult: () => void; onCopyFormula: () => void }) { return <div className="eld-summary"><span><i className="eld-live-dot" />Realtime calculation</span><div><button className="eld-button" onClick={onCopyResult}>{copied === 'result' ? 'Copied' : 'Copy Result'}</button><button className="eld-button is-ghost" onClick={onCopyFormula}>{copied === 'formula' ? 'Copied' : 'Copy Formula'}</button></div></div> }
