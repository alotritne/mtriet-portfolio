export function SummaryCard({ copied, onCopyResult, onCopyFormula, onCopyAudit, onCopyComparison, onCopyFees, onShare, onExportJson, onExportPdf }: { copied: string; onCopyResult: () => void; onCopyFormula: () => void; onCopyAudit: () => void; onCopyComparison: () => void; onCopyFees: () => void; onShare: () => void; onExportJson: () => void; onExportPdf: () => void }) { const action = (id: string, label: string, handler: () => void) => <button className="eld-button is-ghost" onClick={handler}>{copied === id ? 'Copied.' : label}</button>; return <div className="eld-summary"><span><i className="eld-live-dot" />Realtime calculation</span><div>{action('result', 'Copy Result', onCopyResult)}{action('formula', 'Copy Formula', onCopyFormula)}{action('audit', 'Copy Audit', onCopyAudit)}{action('comparison', 'Copy Comparison', onCopyComparison)}{action('fees', 'Copy Fee Breakdown', onCopyFees)}<button className="eld-button" onClick={onShare}>Share Calculator</button><button className="eld-button is-ghost" onClick={onExportJson}>Export JSON</button><button className="eld-button is-ghost" onClick={onExportPdf}>Export PDF</button></div></div> }
