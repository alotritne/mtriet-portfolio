type Props = { bank: string[]; crypto: string[]; copy: () => void }
export function FormulaCard({ bank, crypto, copy }: Props) { return <aside className="eld-card eld-formulas"><div className="eld-card-heading"><div><span className="eld-eyebrow">Audit trail</span><h2>Calculation log</h2></div><button className="eld-quiet-button" onClick={copy}>Copy</button></div><section><h3>Route A <i>Bank</i></h3>{bank.map(item => <code key={item}>{item}</code>)}</section><section><h3>Route B <i>USDT</i></h3>{crypto.map(item => <code key={item}>{item}</code>)}</section></aside> }
